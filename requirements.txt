# Core dependencies (pip install)
PyQt6>=6.5.0
Pillow>=10.0.0
reportlab>=4.0.0
tifffile>=2023.1.1
glymur>=0.12.0
lxml>=4.9.0

# Optional — improves JPEG2000 support
# openjpeg is a system library; glymur will use it automatically if installed
